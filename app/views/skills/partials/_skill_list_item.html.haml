%li.list-group-item{id: "skill_#{skill.id}"}
  .btn-toolbar
    %span= skill.name
    = link_to user_skill_path(current_user.id, skill.id), class: 'btn btn-danger btn-xs pull-right', remote: true, method: :delete do
      Delete
      %span.glyphicon.glyphicon-remove
